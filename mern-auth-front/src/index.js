import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

ReactDOM.render(<App />, document.querySelector("#root")); //we render it like a tag, which will create a react component out of it
//the query selector will insert into root element in index.html, which this returns App


